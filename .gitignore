# Local environment files hold secrets (D-013) and must never be committed.
.env
.env.*
!.env.example

# Build artifacts
target/
node_modules/
web/dist/
.venv/
__pycache__/

# OS noise
.DS_Store

# Ad-hoc screenshots dropped in the repo root while reviewing the UI
/Screenshot*.png
