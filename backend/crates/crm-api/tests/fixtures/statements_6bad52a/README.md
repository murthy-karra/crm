# Slice 019b frozen statements

These files are byte-for-byte SQL snapshots from commit `6bad52a`, before
custom-field filter slots were added. They are test-only inputs for the paired
request performance harness; production code never selects them.
