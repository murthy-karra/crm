# Slice 010e3 — Execution brief

**APPROVED FOR IMPLEMENTATION — D-078, 2026-09-13.** Implements the accepted
[admission specification](../specs/SLICE_010e3.md). See the
[planning review](MOBILE_003_010e3_PLANNING_REVIEW.md) and
[parallel plan](../plans/MOBILE_003_010e3_PARALLEL_LAUNCH.md). Implementation and
isolated synthetic verification are authorized; live source processing is not.

## Objective and ownership

Preview and confirm the admission of qualified People first observed after the
original retained import boundary. Keep global source identity, complete core
values/provenance and the admin review hold. Expose held/excluded records and the
deferred notes/tasks/metadata/history coverage. Do not repair original records,
refresh prior admissions, extend child imports or activate the workspace.

**Proposed branch:** `codex/migration-010e3`. One Terra high primary writer owns
backend and Web in one worktree. Own new `domain/migration/people_admission*`
modules, route implementations, additive admission/identity schema, focused
DB/API tests, new Web API/component/tests and assigned verification evidence.
Reuse original qualification/materialization rules without changing their
meaning. Any necessary shared helper edit has one explicitly assigned writer.

The coordinator owns shared docs, module/router/worker registration, workspace
guards, history union/registration, release capability/preflight, navigation/
Person profile integration, root manifests/scripts and `.sqlx`. Supply precise
patches for sequential integration. Only this lane creates 010e3 migrations;
the Mobile 003 lane owns its separate schema. Reserve versions and merge order
before coding. Do not edit the contact command/mobile or native sources.

## Execution sequence

1. Inventory every reader/writer of `migration_import_identity`, original
   result/contact/provenance readers and history/preview variants. Freeze
   `SLICE_010e3_CONTRACT.md`: exact schema/DTOs, eligibility/dispositions, mapping
   inheritance, shared identity origin/FKs, fact/provenance, private permit,
   lifecycle/request receipts, source boundary, byte ledger and capability.
   Preserve original rows and tombstones; no second uniqueness registry or
   fabricated original import result is allowed.
2. Build bounded source qualification and immutable preview. Independently prove
   exact original absence and qualify complete newer raw observations; report
   labels and representative arrays are not executable authority. Account for
   every report People group, including all-excluded pages and original holds.
   Reuse original mappings only; new/unqualified keys hold the whole Person.
3. Add confirmed per-Person execution: new target, contacts, initial facts,
   provenance, identity, result/receipt and owned ledger settlement commit
   atomically. Recheck live admin, workspace/parent, plan, lease, exact source/
   mapping and capacity. Any occupied target or inconsistent identity is held.
4. Verify paused/cancelled/crashed recovery and the same-boundary remainder plan.
   Confirmed boundaries cannot regress; identities settled before cancellation
   remain consumed. No old parent/sibling result or older native Person changes.
   Keep all reservations/cancel capacity admission-owned.
5. Implement actual Web preparation/preview/confirmation/results/retry/cancel
   using frozen DTOs and bounded escaped data. Add admission provenance and
   compatible Person history rendering through coordinator-owned integration.
   Show the coverage gap on the admission result and admitted Person; do not
   imply existing child-import buttons will enrich it.
6. Exercise isolated production Web/API at desktop and 390px with retained
   synthetic capture/report evidence, exact native reconciliation and partial
   recovery. Record the actual run, failures and inspected screenshots.

## Required checks and completion

The acceptance matrix is spec §7: source qualification/absence, same-contact
distinct People, identity/tombstone/race behavior, original mapping revalidation,
exact previews and source boundary, crash/cancellation/capacity accounting,
least-privilege permits, tenant isolation, bounded complete review and old/new
provenance/history compatibility. Include negative tests showing original
import and 010e2 tokens cannot borrow admission authority or mutate older rows.

Use separate synthetic review-mode database/API/Web resources and build outputs;
preserve shared development and the user's native demo. Assign free ports from a
fresh inventory. No live FUB account or customer dataset is necessary.

Run formatting/Clippy/type and focused interpreter/DB/API/Web tests; one D-050
25k-People/50-member plan pass with sparse admission pages and shared contacts;
one relevant paired Person read; final integrated `scripts/check`, live SQLx
preparation and serialized `scripts/check-db`. The coordinator runs shared gates
once for the final combined source; repeat only affected checks after fixes.
At most two bounded review/fix rounds.

Completion records exact source/runtime/commands, admitted/held/excluded counts,
unchanged original/sibling/native rowsets, charge-once evidence, recovery and
inspected UI. No claim of complete migration, live qualification, activation,
restore testing or deployment follows from synthetic acceptance.

The following migration proposal should extend dependent family coverage to
admitted People and define their later refresh baseline. Mapping repair,
remaining source families, final cutover reconciliation and activation stay
visible separate work; do not silently add them to this implementation.
