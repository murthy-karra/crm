# Slice 010f3 — Execution brief

**APPROVED FOR IMPLEMENTATION — D-082.** [Specification](../specs/SLICE_010f3.md)
and [coordination](../plans/MOBILE_005_010f3_PARALLEL_LAUNCH.md) are accepted following
READY independent planning review. Branch `codex/migration-010f3`; one
Terra high writer owns the bounded migration backend then Web.

## Ownership and execution order

Own new `domain/migration/admitted_metadata*` modules, additive feature/global-claim
schema, assigned original metadata helper/writer changes, focused DB/API tests and
new Web API/panel/tests. No native or mobile command edits. Supply shared guards,
router/worker/preflight and `.sqlx` patches to the coordinator; only this lane
creates 010f3 migrations. Allocate unique versions from actual inventory at launch.

1. Freeze `SLICE_010f3_CONTRACT.md`: exact admission/root/attempt/result FKs,
   selected report/snapshot qualifications, closed states/errors, global catalog
   claim/backfill/provenance and source-key equality, private permit allowlist,
   ledger counted columns, locks, route/DTO/cursor fixtures and recovery capability.
   Freeze the §4 atomic readiness handover: old-writer fencing before enumeration,
   final catch-up/accounting/activation under the namespace barrier, no admitted
   preview of an unready registry, and durable capability before first confirmation.
   Inventory every original metadata writer affected before implementing claims.
2. Implement bounded cohort/source preparation using qualified retained evidence,
   successful terminal results and the existing 010f1 lossless metadata interpreter.
   Keep original-only lookup/FKs intact; use a separate admission resolver. Preview
   freezes source, targets, global claims, native cell baselines and exact effects.
3. Backfill shared catalog claims without rewriting existing identity evidence;
   integrate claims into both original/admitted typed catalog transactions. Verify
   original/multiple-cohort races, collision/tombstone/archival cases and old-binary
   rejection, including old units already running at handover. Failed handover
   rolls back claims and owner charges; successful readiness survives later preview
   cancellation. Avoid speculative generalization of all migration families.
4. Implement typed permit-scoped execution, actor-bound confirmation/receipts,
   bounded leases, byte settlement, explicit retry/cancel and exact unprocessed
   remainder. Prove native rows and results/claims/checkpoints commit together.
5. Freeze Web-facing fixtures, then implement source/cohort selection, mappings,
   counted subset approval, bounded evidence/results, retry/cancel/remainder and
   admitted-Person provenance. Preserve existing child controls and waiting mode.
6. Run real desktop/390px production-Web/API acceptance using isolated retained
   synthetic capture evidence. Reconcile all source/original/admission/native
   rowsets and ledger ownership; classify legitimate derived revisions explicitly.

## Required checks and evidence

Spec §7 is the matrix. Include two Orgs/admins, cancelled/completed admission
cohorts, shared fields/tags with original import, equal/different local values,
multiple capture boundaries and partial catalog/Person cancellation. Verify all
private permits against wrong unit/lease/Org and ordinary direct mobile commands
against the review hold. Source disconnect must not remove retained admin reads.

Run focused backend format/Clippy/unit/DB/API/contract checks and Web lint/type/unit
checks plus actual browser acceptance. D-050 gates new/changed hot statements and
one paired Person/Today read on the realistic book; measure logical retained byte
fan-out separately from physical storage. Do not run an all-maxima benchmark.

Coordinator owns final sequential `scripts/check`, `scripts/sqlx-prepare`,
`scripts/check-db` on integrated source, isolated outputs, and cross-slice upgrade/
guard tests. No concurrent DB gate processes or shared release artifact rebuilds.
Keep evidence with exact runner/source/fixture/commands, failures and preservation
counts in `SLICE_010f3_VERIFICATION.md` when executed. Maximum two review/fix rounds.

This brief authorizes no source call or release. D-082 authorizes isolated
implementation and synthetic schema/verification work; concrete compatible DTO/
SQL checkpoints are owned work; changes to the proposed lifetime, overwrite,
identity or authority policies return to the spec rather than being guessed.
